% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PredictionDataCompRisks.R,
%   R/as_prediction_cmprsk.R
\name{as_prediction.PredictionDataCompRisks}
\alias{as_prediction.PredictionDataCompRisks}
\alias{as_prediction_cmprsk}
\alias{as_prediction_cmprsk.PredictionCompRisks}
\alias{as_prediction_cmprsk.list}
\alias{as_prediction_cmprsk.data.frame}
\title{Convert to a Competing Risk Prediction}
\usage{
\method{as_prediction}{PredictionDataCompRisks}(x, check = FALSE, ...)

as_prediction_cmprsk(x, ...)

\method{as_prediction_cmprsk}{PredictionCompRisks}(x, ...)

\method{as_prediction_cmprsk}{list}(x, ...)

\method{as_prediction_cmprsk}{data.frame}(x, ...)
}
\arguments{
\item{x}{(any)\cr
Object to convert.}

\item{check}{(\code{logical(1)})\cr
Whether to validate internal \code{PredictionDataCompRisks} data during conversion with \code{\link[mlr3:as_prediction]{mlr3::as_prediction()}}.
Use \code{TRUE} for user-supplied data; \code{FALSE} is intended for already validated internal data.
With \code{FALSE}, correct behavior is not guaranteed if the input is invalid.}

\item{...}{(any)\cr
Additional arguments.}
}
\value{
\link{PredictionCompRisks}.
}
\description{
Convert object to a \link{PredictionCompRisks}.
An existing \link{PredictionCompRisks} is returned unchanged.
Use \code{\link[mlr3:as_prediction]{mlr3::as_prediction()}} to convert its internal \code{PredictionDataCompRisks} data.
}
\details{
A named \code{list} must contain \code{row_ids}, \code{truth}, and \code{cif}.
These elements are passed to the \link{PredictionCompRisks} constructor and
validated there (using \code{check = TRUE} by default).
The \code{cif} element must be a nonempty list of matrices, with one matrix for every task cause.
This requirement also applies to internal prediction data.

If \code{x} is a \code{data.frame}/\code{data.table} input, the following requirements must be met:
\itemize{
\item Cols \code{row_ids}, \code{time}, \code{event}, and a list-column \code{CIF} must be present.
\item The per-observation \code{CIF} object must be a named list of numeric vectors
(one per cause), with names exactly \code{"1"}, \code{"2"}, ..., \code{"K"}, in that order
(where \code{K} is the number of competing risks).
\item Cause names should be identical across observations and in the same order;
CIF vectors corresponding to the same cause should be of equal length (same time points).
\item Values for the \code{event} column must be 0 (censoring) or one of the consecutive
cause codes 1, 2, ..., K.
}
}
\examples{
library(mlr3)
task = tsk("pbc")
learner = lrn("cmprsk.aalen")
learner$train(task)
p = learner$predict(task)

# from an existing prediction
as_prediction_cmprsk(p)

# convert a named list
as_prediction_cmprsk(list(row_ids = p$row_ids, truth = p$truth, cif = p$cif))

# from a data.frame
tab = as.data.frame(as.data.table(p))
as_prediction_cmprsk(tab)

# from internal prediction data
as_prediction(p$data, check = TRUE)
}
